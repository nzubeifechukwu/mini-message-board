<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Home | <%= title %></title>
    <link rel="stylesheet" href="styles.css" />
  </head>
  <body>
    <header>
      <nav><%- include('navbar', {links: links}) %></nav>
      <h1><%= title %></h1>
    </header>
    <main>
      <% if (messages.length) { %> <% messages.forEach((message) => { %>
      <article>
        <h2><%= message.username %></h2>
        <p><%= message.message %></p>
        <p><%= message.date_added %></p>
        <a href="/<%= message.user %>/message">Open</a>
      </article>
      <%});%> <%} else { %>
      <p>No messages found. Click on New Message to add a message</p>
      <%} %> %>
    </main>
    <footer><%- include('footer') %></footer>
  </body>
</html>
